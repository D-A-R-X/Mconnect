<?xml version="1.0" encoding="utf-8"?>
<!-- Active segment pill — solid darker blue (same as the header gradient
     end) so it pops against the lighter outer container. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#02499D" />
    <corners android:radius="18dp" />
</shape>
