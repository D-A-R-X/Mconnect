<?xml version="1.0" encoding="utf-8"?>
<!-- Grouping card for a CP-creation form section (Client Details / CP
     Details). A faint fill + border so it reads as a container behind the
     white input pills. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#F7F8FA" />
    <stroke android:width="1dp" android:color="#EDF0F4" />
    <corners android:radius="16dp" />
</shape>
