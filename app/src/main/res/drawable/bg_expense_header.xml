<?xml version="1.0" encoding="utf-8"?>
<!--
  Expense screen header background. Flat brand blue with rounded bottom
  corners so the header reads as its own rounded card; the white totals
  card floats up over its lower edge, exposing the rounded "shoulders"
  of the blue on either side — matches the reference design.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/category_labour" />
    <corners
        android:bottomLeftRadius="24dp"
        android:bottomRightRadius="24dp"
        android:topLeftRadius="0dp"
        android:topRightRadius="0dp" />
</shape>
