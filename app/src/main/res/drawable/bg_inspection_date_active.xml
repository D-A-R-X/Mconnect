<!-- Active state for the inspection date-filter button: solid blue with the
     same 10dp radius as the search bar so the paired set stays consistent. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#0B61CA" />
    <corners android:radius="10dp" />
</shape>
