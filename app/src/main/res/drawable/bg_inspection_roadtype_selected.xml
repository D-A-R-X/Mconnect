<!-- Road-type checkbox row in selected state — soft blue tint with a
     blue border (matches the reference design). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#E0EBFB" />
    <stroke
        android:width="1dp"
        android:color="#0B61CA" />
    <corners android:radius="10dp" />
</shape>
