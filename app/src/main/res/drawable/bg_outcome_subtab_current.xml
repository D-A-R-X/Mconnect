<?xml version="1.0" encoding="utf-8"?>
<!-- "Current step" sub-tab pill — green outline, white fill, green text.
     Used for the sub-tab that is currently being filled (not yet
     completed). Completed tabs use bg_outcome_subtab_active (green fill);
     upcoming tabs use bg_outcome_subtab_inactive (neutral outline). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#FFFFFF" />
    <stroke
        android:width="1.5dp"
        android:color="#2DAE12" />
    <corners android:radius="20dp" />
</shape>
