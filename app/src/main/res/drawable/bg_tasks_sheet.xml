<?xml version="1.0" encoding="utf-8"?>
<!-- Content sheet for the My Tasks page. Light-grey (same as the page bg)
     so the white task cards + white search pill read as distinct layers,
     matching the design. Rounded top lifts over the blue header. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#F1F3F8" />
    <corners
        android:topLeftRadius="24dp"
        android:topRightRadius="24dp"
        android:bottomLeftRadius="0dp"
        android:bottomRightRadius="0dp" />
</shape>
